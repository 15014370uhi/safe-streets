import React, {useState} from 'react';
//import ToggleButton from '@material-ui/lab/ToggleButton';
//import '../App.css';

// TODO - help reusability by prop the button text etc


const FilterButton = () => {

// Set state for each value of filterName 
const [filterName, setFilterName] = useState ('');

  // Set state for each value of title and mapURL
  //const [title, setTitle] = useState ('');  // TODO check if need state for active not active


    return (
      <div></div>
    );
  };
  
  export default FilterButton;